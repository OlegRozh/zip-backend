services:
  zip-backend:
    build: .
    ports:
      - '8080:8080'

  prometheus:
    image: prom/prometheus:v2.51.0
    container_name: prometheus
    ports:
      - '9090:9090'
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    depends_on:
      - zip-backend
